/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // Allow image domains if you want to use next/image (optional)
  images: {
    domains: ['profile.thug4ff.com'],
  },
}

module.exports = nextConfig